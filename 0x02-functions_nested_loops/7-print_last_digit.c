#include "main.h"

/**
 * print_last_digit -> the last digit of a number
 * @int: the passed args
 * Return: the last digit
 */
int print_last_digit(int)

{
	if (n < 0)
		n = -n;
	x = n % 10;
	_putchar(x + '0')
	return (x);
}
