#include "main.h"

/**
 * _abs -> a function that compute the absolute value of an integer
 * @n: parameter
 * Return: absolute value of a number
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}

