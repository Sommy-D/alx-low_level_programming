#included "main.h"

/**
 * _islower -> checks for lowercase character
 * @c - a character argument
 * return: returns 1 and 2 depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

